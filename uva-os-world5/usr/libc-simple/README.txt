a tiny lib for "simple userspace"
no dep on libc etc

supported apps: 
buzz
nes1
cmdline utilities (sh, ls ...)

printf.c: for printf() & family. from xv6

tinyprintf.c: for sprintf(), which is still simple (e.g. no snprintf())